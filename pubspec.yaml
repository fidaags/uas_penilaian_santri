name: e_penilaian_santri
description: Aplikasi e-Penilaian Santri Pesantren
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_riverpod: ^2.5.0

  # Firebase (PAKAI SATU firebase_core SAJA)
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0

  # Secure storage (HANYA 1x)
  flutter_secure_storage: ^9.0.0

  # Routing
  go_router: ^14.0.0

  # PDF
  pdf: ^3.11.0
  printing: ^5.13.0

  # Chart
  fl_chart: ^0.69.0

  # Util
  intl: ^0.19.0

  # Model & JSON
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0

  # Icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
